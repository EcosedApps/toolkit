name: ectoolkit
description: "ECToolkit"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_ecosed:
    path: ../flutter_ecosed
  flutter_boost:
    git:
        url: 'https://github.com/alibaba/flutter_boost.git'
        ref: '5.0.2'
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true